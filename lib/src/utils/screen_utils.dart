import 'package:flutter/cupertino.dart';
import 'package:fly_dash/main.dart';

double get screenWidth => MediaQuery.of(globalContext!).size.width;

double get screenHeight => MediaQuery.of(globalContext!).size.height;

double get statusBarHeight => MediaQuery.of(globalContext!).padding.top;
